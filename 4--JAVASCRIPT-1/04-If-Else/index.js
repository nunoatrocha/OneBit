
const idade = prompt("Diga a sua idade: ")
if (idade >= 16) {
    alert("É maior de idade.")
}
    else {
        alert("Ainda é menor de idade.")
 
    }

const resultado = (6 === 7) ? "Verdadeiro" : "Falso"

alert(resultado)