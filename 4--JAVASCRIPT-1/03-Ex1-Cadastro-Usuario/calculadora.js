
const primeiroNumero = parseFloat(prompt("Digite o primeiro numero: "))
const segundoNumero = parseFloat(prompt("Digite o segundo numero: "))
const soma = primeiroNumero + segundoNumero
const subtracao = primeiroNumero - segundoNumero
const multiplicacao = primeiroNumero * segundoNumero
const divisao = primeiroNumero / segundoNumero

alert("Resultado da SOMA: " + soma + "\nResultado da SUBTRACAO: " + subtracao + "\nResultado da MULTIPLICACAO: " + multiplicacao + "\nResultado da DIVISAO: " + divisao )


/*
let weight

console.log(typeof weight)

let name = "Nuno"
let age = 43
let weight = 73.5
let isSubscribed = true

let student = {
    name: "Nuno",
    age: 43,
    weight: 73.5,
    isSubscribed: true,
}

const pedro = {
    name: "Pedro",
    age: 32,
    weight: 53.5,
    isSubscribed: false,
}

console.log(`${student.name} de idade ${student.age} pesa ${student.weight} kg`)

students = [
    pedro, 
    student
]

console.log(students)
console.log(students[0])

*/


novoObjeto = {
    name: "Nuno",
    age: 43,
}

novoArray =[
    "Fcporto",
    "SLBenfica",
]

